{{#each acoGet}}
<div class="d-inline-flex p-2">
        <div class="card" style="width: 18rem;">
                <div class="card-body">
                        <h5 class="card-title">{{ANOME}}</h5>
                        <p class="card-text">{{ADESCRICAO}}</p>
                        <a href="#" class="btn btn-primary">SELECIONAR</a>
                </div>
        </div>
</div>
{{/each}}