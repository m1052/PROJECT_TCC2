{{#each acoGet}}
<div class="d-inline-flex p-2">
        <div class="card" style="width: 18rem;">
                <div class="card-body">
                        <h4 class="card-title">{{ACODIGO}}</h4>
                        <b><p>{{ANOME}}</p></b>
                        <p class="card-text">{{ADESCRICAO}}</p>
                        <a href="{{>selecionarLink}}/eletrodo/aco/{{ACODIGO}}" class="btn btn-primary">SELECIONAR</a>
                </div>
        </div>
</div>
{{/each}}
{{#each getRelSitAco}}
<div class="d-inline-flex p-2">
        <div class="card h-100" style="width: 18rem;">
                <div class="card-body">
                        <h4 class="card-title">{{ACODIGO}}</h4>
                        <b><p>{{ANOME}}</p></b>
                        <p class="card-text">{{ADESCRICAO}}</p>
                        <a href="{{>selecionarLink}}/eletrodo/{{ACODIGO}}" class="btn btn-primary">SELECIONAR</a>
                </div>
        </div>
</div>
{{/each}}